apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv
spec:
  capacity:
    storage: 20Gi  # 根据您的需求调整大小
  accessModes:
    - ReadWriteMany
  nfs:
    server: <nfs_server_ip>  # 替换为您的 NFS 服务器 IP
    path: "/opt/module/nfs/data/shared"  # NFS 服务器的共享路径