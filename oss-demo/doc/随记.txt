概述
    OSS （ Object Storage Service ）是对象存储服务，是一种云存储架构。
核心概念
    对象
        对象是 OSS 存储的存储单元，每个文件都是一个对象。
    存储桶（ bucket ）
        存储桶是 OSS 存储的逻辑命名空间，类似于文件夹，该文件夹内的文件数量不受限制，但是只能有一层层级结构。
        存储桶除了用于逻辑存储，还可以实现访问权限控制、生命周期管理等功能。